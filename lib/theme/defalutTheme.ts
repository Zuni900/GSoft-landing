import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#f3e5f5",
      light: "#F5F5F5",
    },
    // for header
    secondary: {
      main: "#001A1F",
      light: "#ffffff",
      contrastText: "#16B1E1",
    },
  },
});
